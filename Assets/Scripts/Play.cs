using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Play : MonoBehaviour
{
    public string ID;
    public float playStart;
    public string playBatchName;
    public bool PlayFixationFlag, PlayCLFlag, PlayEyeFlag, PlayShimmarFlag, PlayEEGFlag;
    public string key;
    public string supposedPlayKey;
    public string hitormiss;
    public float keyTime;
    public bool PlayedFixationFlag, PlayedCLFlag, PlayedEyeFlag, PlayedShimmarFlag, PlayedEEGFlag;
    // Start is called before the first frame update

   
}
